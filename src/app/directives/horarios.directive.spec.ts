import { HorariosDirective } from './horarios.directive';

describe('HorariosDirective', () => {
  it('should create an instance', () => {
    const directive = new HorariosDirective();
    expect(directive).toBeTruthy();
  });
});
