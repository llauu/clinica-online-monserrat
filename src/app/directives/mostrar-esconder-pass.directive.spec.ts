import { MostrarEsconderPassDirective } from './mostrar-esconder-pass.directive';

describe('MostrarEsconderPassDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarEsconderPassDirective();
    expect(directive).toBeTruthy();
  });
});
