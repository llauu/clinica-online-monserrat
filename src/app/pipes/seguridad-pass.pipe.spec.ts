import { SeguridadPassPipe } from './seguridad-pass.pipe';

describe('SeguridadPassPipe', () => {
  it('create an instance', () => {
    const pipe = new SeguridadPassPipe();
    expect(pipe).toBeTruthy();
  });
});
